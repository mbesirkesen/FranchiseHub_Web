import type { NextConfig } from "next";

/** API yönlendirmesi `src/app/api/proxy/[...path]/route.ts` üzerinden yapılır (Authorization ve gövde birebir iletilir). */
const nextConfig: NextConfig = {};

export default nextConfig;
